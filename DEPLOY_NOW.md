# 直接部署步骤

1. 下载并解压本项目包。
2. 打开 GitHub 仓库 `ai-river-biology`。
3. 点击 `Add file → Upload files`。
4. 把本项目包解压后的所有文件拖入仓库根目录。
5. 点击 `Commit changes`。
6. 打开 Streamlit Cloud，选择该仓库。
7. `Main file path` 填：`app.py`。
8. 点击 `Deploy`。

如果你已经部署过旧版本，上传覆盖后 Streamlit 通常会自动重启。若没有自动更新，请点击右下角 `Manage app → Reboot app`。
