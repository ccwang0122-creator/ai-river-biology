# 数据集构建说明

内置训练库由用户提供的 `已命名.pdf` 提取而来。PDF 每页包含若干显微图片及对应中文属名/类群标签。构建流程：

1. 读取 PDF 每页内嵌图片；
2. 读取页面文字标签；
3. 按页面顺序把图片与标签对应；
4. 生成 `reference_images/`、`reference_metadata.csv` 和 `model/reference_features.npz`。

由于少数页面存在标签数量与图片数量不完全一致的情况，构建时只使用能明确匹配的图片—标签对。
